# ComfyUI 接入 · 剪映文生视频教程脚本

> 适用于剪映「AI 创作 · 文生视频」：逐条复制画面提示词生成镜头，拖入轨道拼接；
> 配音用剪映「文本朗读」粘贴配音文案生成。总时长约 100 秒。
> 对应功能：设置里的 ComfyUI 模型提供商，以及图片 / 视频 / 声音节点的本机出图出片（含 MiniMax H3）。

## 使用说明

1. **生成画面**：打开剪映 → AI 创作 → 文生视频 → 把下方某条「画面提示词」整段复制粘贴 → 生成，重复此步骤得到全部镜头。
2. **拼接**：按镜头编号顺序把生成结果拖入轨道；涉及软件界面与命令的镜头（见「录屏镜头」）用实际录屏片段，不用 AI 生成。
3. **配音**：新建文本 → 把对应「配音文案」粘贴到文本朗读 → 生成旁白拖到音频轨。
4. **字幕**：用每条「字幕要点」做字幕条。
5. **一致性**：所有镜头提示词都以「现代科技感 AI 创作工作室、深色界面、柔光、电影感」开头；出现人物时把同一张人物参考图作为首帧/参考图上传，保证角色统一。

## 一、AI 生成镜头

### 镜头 1（约 6 秒）

画面提示词（复制整段）：

> 现代科技感 AI 创作工作室，深色桌面，创作者面前摆着两台显示器，左屏是满屏节点的 ComfyUI 工作流，右屏是 AIArtEngine 的节点画布，两台屏幕之间悬浮一个红色断开图标，创作者皱眉挠头，镜头缓慢推近，柔光，电影感，画面清晰。

配音文案：

> 本地装好了 ComfyUI，模型也下好了，想在这套工作流里直接跑，却发现怎么都接不上。

字幕要点：

> 本地 ComfyUI 接不上？

### 镜头 2（约 7 秒）

画面提示词（复制整段）：

> 现代科技感 AI 创作工作室的屏幕特写，深色界面里一条发光连线从写着 8188 的端口出发，经过一个齿轮形状的代理盒子，再连到写着 8189 的端口，旁边的文字从 /prompt 切换成 /api/v2/jobs，镜头沿连线匀速推进，柔光，电影感，画面清晰。

配音文案：

> 本软件只走 ComfyUI 的 API 2，8188 是旧接口，中间加一个 comfy-api-proxy，把 8188 转成 8189 就行。

字幕要点：

> 8188 → 代理 → 8189（API 2）

### 镜头 3（约 8 秒）

画面提示词（复制整段）：

> 现代科技感 AI 创作工作室，深色终端窗口占满屏幕，一行 pip install comfy-api-proxy 命令被输入，回车后安装进度条快速滚动，随后输入启动命令，终端底部浮现绿色的服务已就绪状态，镜头稳定近景，柔光，电影感，画面清晰。

配音文案：

> Python 环境里一条命令装好代理，启动后访问 8189 的 health，返回就绪，接口就通了。

字幕要点：

> pip install comfy-api-proxy，访问 /api/v2/health

### 镜头 4（约 8 秒）

画面提示词（复制整段）：

> 现代科技感 AI 创作工作室的屏幕特写，ComfyUI 界面里打开设置并点亮开发者模式开关，菜单里浮现 Save (API Format) 选项，点击后导出一个 JSON 文件，文件名被高亮放大，镜头聚焦文件名，柔光，电影感，画面清晰。

配音文案：

> workflow 要存成 API 格式，模型 id 就是文件名；放进 userdata 目录，软件按名字去取。

字幕要点：

> Save (API Format)，模型 id = 文件名

### 镜头 5（约 8 秒）

画面提示词（复制整段）：

> 现代科技感 AI 创作工作室的屏幕特写，AIArtEngine 设置界面的模型提供商列表里出现 ComfyUI 卡片，输入框填入 8189，点击拉取模型后图片、视频、声音三个页签依次点亮并被勾选，镜头从左到右扫过界面，柔光，电影感，画面清晰。

配音文案：

> 设置里加 ComfyUI，Base URL 填 8189，本机不用 Key，拉取模型后勾选要用哪几个。

字幕要点：

> 设置 → ComfyUI → 8189 → 拉取并勾选

### 镜头 6（约 8 秒）

画面提示词（复制整段）：

> 现代科技感 AI 创作工作室的屏幕特写，节点画布上一个图片生成节点，模型下拉选 txt2img，输入框里写下一句指令，点击执行后任务队列开始滚动，片刻后一张清晰的图片从节点里浮现，镜头聚焦图片，柔光，电影感，画面清晰。

配音文案：

> 图片节点选 txt2img，写一句指令，执行，任务队列里提交再轮询，图就出来了。

字幕要点：

> 选 txt2img → 写指令 → 执行出图

### 镜头 7（约 9 秒）

画面提示词（复制整段）：

> 现代科技感 AI 创作工作室的屏幕特写，节点画布上一个视频生成节点，模型下拉选 MiniMax H3 图生视频，一张参考图被拖入并连到节点的首帧口，参数选好 16:9 和时长后点击执行，一段带有声音的视频从节点里缓缓生成，镜头聚焦成片，柔光，电影感，画面清晰。

配音文案：

> 视频用 MiniMax H3，图生视频连首帧，软件自动对齐 32 倍数、压到原生画布，时长也按 24 帧网格算好，点执行直接出片。

字幕要点：

> MiniMax H3 图生视频，自动对齐尺寸与帧数

### 镜头 8（约 6 秒）

画面提示词（复制整段）：

> 现代科技感 AI 创作工作室，创作者面对镜头满意地微笑，身后的屏幕上 ComfyUI 节点图与一段 AIArtEngine 成片画面交替滚动，屏幕下方浮现订阅按钮图标，镜头匀速后拉，柔光，电影感，画面清晰。

配音文案：

> 本机模型免 Key 随便跑，云端填 cloud.comfy.org 也能接。想看哪个 workflow，评论区告诉我。

字幕要点：

> 本机免 Key，云端也能接

## 二、录屏镜头（软件界面用实际录屏，不用 AI 生成）

剪映文生视频生成文字和界面容易乱码，以下镜头请用真实软件录屏：

| 镜头 | 录屏内容 | 对应脚本 |
| --- | --- | --- |
| A | 终端执行 pip install comfy-api-proxy、启动代理、浏览器访问 /api/v2/health 返回就绪 | 对应镜头 3 |
| B | ComfyUI 开 Dev mode → Save (API Format) → 保存到 user/default/workflows/ | 对应镜头 4 |
| C | 设置 → 模型 → 添加 ComfyUI → Base URL 填 8189 → 拉取可用模型并勾选 | 对应镜头 5 |
| D | 图片节点选 txt2img → 输入指令 → 执行 → 任务队列提交与轮询出图 | 对应镜头 6 |
| E | 视频节点选 MiniMax H3 图生视频 → 连首帧 → 选 16:9 与时长 → 执行出片 | 对应镜头 7 |

## 三、拼接与配音顺序

1. 时间轴顺序：镜头 1 → 2 → 3 → 录屏 A → 4 → 录屏 B → 5 → 录屏 C → 6 → 录屏 D → 7 → 录屏 E → 8。
2. 镜头之间加淡入淡出转场，避免 AI 生成的人物长相漂移；需要更稳时可每段都上传同一张人物参考图。
3. 配音用「文本朗读」按配音文案逐段生成，拖到音频轨后与画面对齐；字幕用每条字幕要点。
4. 端口号、命令、菜单名等文字信息优先用录屏或静态卡片展示，避免 AI 生成乱码。

## 四、与前面几期的衔接

- 前面几期讲了节点生成、导演台、成片时间线；这一期补上「本地模型怎么接进来」，形成完整闭环。
- 结尾 CTA 可互相引导：「想看 MiniMax H3 首尾帧、多模态参考怎么玩，点进视频生成指南那期」。
